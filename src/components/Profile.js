import React, { PropTypes } from 'react'

const Profile = React.createClass({
  render () {
    return (
      <div>
        Put user profile page here
      </div>
    )
  }
})

export default Profile
