import React, { PropTypes } from 'react'

// API Documentation
const API = React.createClass({
  render () {
    return (
      <div id="api">
        Put API documentation here
      </div>
    )
  }
})

export default API
