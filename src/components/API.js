import React, { PropTypes } from 'react'

// API Documentation
const API = React.createClass({
  render () {
    return (
      <div>
        Put API documentation here
      </div>
    )
  }
})

export default API
