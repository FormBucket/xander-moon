import React, { PropTypes } from 'react'

const Submissions = React.createClass({
  render () {
    return (
      <div>
        Put submission admin screen here
      </div>
    )
  }
})

export default Submissions
