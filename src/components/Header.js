import React, { PropTypes } from 'react'

const Header = React.createClass({
  render () {
    return (
      <div id="header">
        Header goes here
      </div>
    )
  }
})

export default Header
